<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des articles</title>
</head>
<body>
    <h1>Liste des articles</h1>

    <a href="/article/new">Ajouter un nouvel article</a>

    <ul>
        {% for article in articles %}
            <li>
                <h3>{{ article.title }}</h3>
                <p>{{ article.content }}</p>
                <small>Écrit par {{ article.username }}</small>
            </li>
        {% endfor %}
    </ul>
</body>
</html>
